server:
  servlet:
    session:
      cookie:
        name: SESSION
  error:
    include-exception: on
spring:
  jpa:
    open-in-view: false
  output:
    ansi:
      enabled: detect
  web:
    locale: zh_CN
  flyway:
    baseline-on-migrate: true
  data:
    redis:
      client-name: luttece